# Multi-module wiring on sbt 2: shared + per-module merge, exact-first binding, unlisted isolation.
> checkCoreName
> checkCoreDeps
> checkSkunkDeps
> checkAnalyticsExactFirst
> checkUnlisted

# core resolves its (shared) dependencies and compiles.
> core/compile
